
export default function(tellClient)
{
const addGame = (req, res, next) => {};

const allGames = (req, res, next) => {};

const editGame = (req, res, next) => {};

return {addGame,allGames,editGame}
}
